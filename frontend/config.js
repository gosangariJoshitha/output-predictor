// API Configuration
// Replace the production URL with your actual Render backend URL once deployed
export const API_BASE_URL = window.location.hostname === 'localhost' 
  ? 'http://localhost:5000'
  : 'https://your-backend-app.onrender.com'; // TODO: Update this
